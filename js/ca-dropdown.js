$(function(){

	var menuWindow = $('.ca-menu-window');
	var menuContainer = $('.ca-menus-container');
	var menu = $('.ca-menu');

	//Set container width to equal *2 the rendered window width;
	alert(menuWindow.width() + ', ' + menu.width() + ', ' + menuContainer.width());

	//Set menu width to equal the window frame width

});